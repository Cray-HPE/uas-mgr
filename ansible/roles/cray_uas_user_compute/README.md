# Cray Shasta cray_uas_user_compute ansible role

This ansible role adds a user to compute nodes for UAS testing.

## Role variables

The users to add will be gathered from cray_uas_user/defaults/main.yml
