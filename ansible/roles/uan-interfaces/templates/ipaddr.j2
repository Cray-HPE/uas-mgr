{% for node in static_ips.uan.uan_nodes %}
    {%- if ansible_hostname == node.hostname -%}
        IPADDR='{{ (node.ip, static_ips.uan.netmask) | join('/') | ipaddr() }}}}'
    {%- endif -%}
{% endfor %}
